# 옆커폰 경매매물 사이트 — 작업 이어하기 (핸드오프)

> 이 파일 하나만 읽으면 지금까지 뭘 했고 다음에 뭘 하면 되는지 다 안다.
> **여기 없는 사실은 지어내지 말 것.**

- 폴더: `옆커폰_경매물건_사이트/`
- 결과물: 정적 HTML 7장(`index / property / checkout / success / fail / about / courses`) + Cloudflare Pages Functions 2개
- 이전 프로젝트(`옆커폰부동산에듀` 교육원 사이트, 같은 폴더 상위에 `index.html`)와는 **완전히 별개**의 신규 사이트. 다만 회사정보·블로그는 동일 인물(고호정) 것을 재사용.
- 마지막 작업일: 2026-07-27
- **로고 실제 파일 적용 완료**: `assets/logo-dark.png`(흰색, 어두운 배경용 — 헤더/푸터에 사용 중), `assets/logo-white.png`(검정, 밝은 배경용 — 예비).
  참고 사이트 `https://gkinvestmnet.imweb.me`의 실제 CDN 주소에서 다운로드함 (naver 블로그와 달리 imweb 도메인은 이 세션에서 접속 차단 없었음).
- **회사소개(`about.html`), 강의안내(`courses.html`) 페이지 신규 추가** — 아래 8-1, 8-2 참고.

---

## 1. 이게 무슨 프로젝트인가

팀장님 지시: "블로그에서 경매 매물을 가져와 홈페이지 메인에 노출 + 상세정보 + 결제까지" 되는 사이트를
3일 안에 제작. 매물 = 부동산 경매 매물. 소스 블로그 = **`blog.naver.com/ykphone_edu`**
(주의: 처음엔 이전 프로젝트 문서를 보고 `hjko0`로 추정했으나, 사용자가 직접 `ykphone_edu`가 맞다고
확인해줌 — 코드 전체 이 아이디로 수정 완료).

## 2. 아키텍처

이전 프로젝트(정적 HTML 한 장)와 달리 **자동 스크래핑 + 실결제 승인**이 들어가서 서버 로직이 필요함.
→ **Cloudflare Pages + Functions**로 구성 (정적 파일은 무료 호스팅, `/functions`는 서버리스 함수).

```
옆커폰_경매물건_사이트/
├── index.html          메인 — 매물 리스트 (fetch /api/properties)
├── property.html       매물 상세 (?id=글번호)
├── checkout.html        토스페이먼츠 결제위젯
├── success.html         결제 성공 → /api/payments/confirm 호출
├── fail.html             결제 실패/취소
├── assets/style.css      공통 스타일 (검정+골드 톤, 이전 사이트와 동일 계열)
├── data/mock-properties.json   목업 데이터 (RSS 연동 전 프리뷰용)
└── functions/api/
    ├── properties.js            GET — 네이버 블로그 RSS 파싱해 매물 목록 반환
    └── payments/confirm.js      POST — 토스 결제 승인(secretKey로 서버측 confirm)
```

## 3. ✅ 지금까지 완료 & 검증한 것

- 메인/상세/체크아웃/성공/실패 5페이지 전부 작성, 검정+골드 톤 유지
- `index.html`, `property.html`은 `/api/properties` 우선 호출 → 실패 시 `data/mock-properties.json`으로
  자동 폴백 (서버 없이 파일로 열어도 미리보기 가능)
- **브라우저로 렌더 확인 완료**: 메인 카드 렌더링, 상세페이지 정보 표시, 체크아웃 결제위젯 정상 표시
  (결제수단 목록·약관 체크박스까지 실제 렌더됨), 성공 페이지 결과 표시 — 전부 스크린샷으로 확인함
- **토스페이먼츠 테스트 연동 키 확정 및 검증 완료**:
  - clientKey: `test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm`
  - secretKey: `test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6`
  - (주의) 처음에 `test_ck_...` 형태의 "API 개별연동 키"를 잘못 써서 위젯 로딩 에러 발생 →
    위젯 연동은 반드시 `test_gck_/test_gsk_` (widget 전용 키)를 써야 함. 확인 후 교체 완료.
  - 이 키들은 토스 공식 문서에 공개된 "계약 전 테스트용" 키라 실제 가맹점과 무관하고 실결제 안 됨.
- `functions/api/properties.js`: `rss.blog.naver.com/ykphone_edu.xml` RSS를 정규식으로 파싱해 제목/링크/
  날짜/썸네일을 추출하고, 제목+본문에 경매 관련 키워드(경매/낙찰/감정가/최저가/물건/입찰)가 있는 글만 필터링
- `functions/api/payments/confirm.js`: 토스 공식 confirm API 스펙대로 Basic Auth + POST 구현
- **실제 블로그 글 구조 확인 완료** (사용자가 PDF로 실제 글 1건 전달 — `blog.naver.com/ykphone_edu/224356311370`,
  "대구 읍내동 아파트 경매 칠곡이편한세상..."). 확인된 패턴:
  - 본문 문장으로 텍스트 추출 가능: "오늘 살펴볼 물건은 {지역}에 위치한 {단지명 동/호}입니다" (주소),
    "감정가는 {금액}이지만 현재 최저매각가격은... {금액}까지 내려왔습니다" (감정가/최저가, "3억 1,000만원"
    같은 한글 혼합 표기)
  - 글 끝 해시태그(`#칠곡이편한세상경매 #읍내동아파트경매 #대구북구아파트경매` 등)로 지역/카테고리 태깅 가능
  - **사건번호·매각기일·보증금·등기부 내역은 법원경매정보 사이트를 캡처한 이미지로 박혀있어 텍스트로
    스크래핑 불가능** → UI에서 이 항목들은 자동 추출하지 않고 "블로그 원문에서 확인" 링크로 대체함
    (거짓으로 채워 넣지 않기 위한 의도적 설계)
- 위 구조에 맞춰 `properties.js` 재작성함: RSS 요약이 짧아 가격/주소 문장이 잘려있으면 해당 글의
  모바일 버전(`m.blog.naver.com/PostView.naver?blogId=ykphone_edu&logNo=...`)을 한 번 더 fetch해서
  전체 본문 텍스트로 재시도하는 2단계 방식으로 구현. (이 fetch가 실제로 성공하는지는 이번 세션 도구
  제약으로 검증 못 함 — 아래 4번 참고)
- `data/mock-properties.json`도 이 실제 글 데이터로 교체 (썸네일 제외 전부 실제 값)
- 푸터 이메일을 실제 명함 이미지에서 확인한 `rhghwjd12@naver.com`으로 수정함 (이전 프로젝트 문서엔
  `hjko0219@kakao.com`으로 되어있었는데, 이번에 받은 블로그 글의 명함엔 다른 이메일이 적혀있었음 —
  같은 회사·같은 대표인데 문서마다 이메일이 다르니 **팀장님께 어느 쪽이 맞는지 확인 필요**)

## 4. ⚠️ 이번 세션의 도구 제약 (중요, 다음 세션도 겪을 수 있음)

- 이 세션의 브라우저/웹조회 도구가 **`blog.naver.com`, `rss.blog.naver.com`, `m.blog.naver.com` 도메인
  접속을 정책상 차단**함. 사용자가 실제 글 링크를 줬을 때도 못 열었고, 결국 사용자가 **PDF로 글 내용을
  직접 캡처해서 보내줘서** 구조를 파악할 수 있었음 (그 PDF는 Windows 내장 WinRT PDF 렌더러로 PNG
  변환해서 이미지로 읽음 — poppler/pdftoppm이 이 환경에 없어서 일반 PDF 도구가 안 먹혔음).
  → `properties.js`의 텍스트 추출 정규식은 실제 글 1건으로 검증했지만, **RSS 요약이나 모바일 페이지를
  실제로 fetch하는 부분(`fetchFullPostText`)은 이 환경에서 한 번도 실행해보지 못함.** 배포 후 반드시
  실제 동작 확인 필요 — naver가 서버 UA를 막거나 다른 HTML 구조를 줄 가능성 있음.
- 이 환경엔 **Node.js/npm/python이 설치되어 있지 않아** `wrangler pages dev`로 로컬 서버를 못 띄웠다.
  → `/api/properties`, `/api/payments/confirm` 두 Function은 **코드 리뷰로만 검증**했고, 실제 실행은
  안 해봤다. Cloudflare 배포 후 반드시 실제 동작 확인 필요.

## 5. ⏳ 남은 일 (다음 세션에서 이어서, 우선순위 순)

1. **Cloudflare Pages 배포 후 `/api/properties` 실제 동작 확인 (최우선)** — 이번 세션 도구로는
   naver 도메인 자체가 막혀서 RSS fetch, 모바일 페이지 fetch 둘 다 한 번도 실행 못 해봄. 배포 직후
   가장 먼저 이 엔드포인트를 열어서 실제 매물이 몇 건 나오는지, 주소/감정가/최저가가 잘 뽑히는지
   확인할 것. 안 뽑히면 `extractAddress`/`extractPrice`의 정규식을 다른 글 샘플로 다시 튜닝.
2. **이메일 불일치 확인** — `hjko0219@kakao.com`(이전 프로젝트 문서) vs `rhghwjd12@naver.com`(이번에
   받은 블로그 글 명함) 중 어느 게 맞는 연락처인지 팀장님께 확인. 지금은 후자로 넣어둠.
3. **결제 실계약 전환** — 지금은 토스 "계약 전 테스트 키"로 위젯만 붙어있는 상태. 팀장님이 실제
   토스페이먼츠 가맹점 가입 후 clientKey/secretKey를 주면: `checkout.html`의 `TEST_CLIENT_KEY`와
   `functions/api/payments/confirm.js`의 `TEST_SECRET_KEY`(또는 env var `TOSS_SECRET_KEY`)만 교체하면 됨.
   secretKey는 절대 클라이언트 코드에 넣지 말고 Cloudflare Pages 환경변수로 설정할 것.
4. **결제 승인 후 실제 처리** — 지금 `success.html`은 승인 결과만 화면에 보여줌. 실제로는 승인 후
   관리자에게 알림(카카오톡/문자/이메일)이 가거나 DB에 신청 기록이 남아야 실무에 쓸 수 있음 — 필요
   여부 팀장님께 확인.
5. ~~매물 상세 사진 여러 장~~ → **완료.** `properties.js`의 `extractImages()`가 본문(또는 RSS 요약) HTML에서
   `pstatic.net` 계열 `<img>` 주소를 최대 15장까지 모아 `images` 배열로 반환하도록 수정함. 아이콘/스티커류는
   `IMG_EXCLUDE` 정규식으로 걸러냄. `property.html`에 갤러리(메인 사진 + 썸네일 클릭 전환) 추가하고 mock
   데이터(가짜 SVG 3장)로 렌더 확인 완료. **단, 실제 네이버 이미지 주소로는 검증 못 함** (naver 도메인
   차단 때문) — 배포 후 진짜 사진이 몇 장 잡히는지, `pstatic.net` 필터가 실제 사진과 아이콘을 잘
   구분하는지 확인 필요. 법원경매 데이터 캡처 이미지도 이 갤러리에 같이 섞여 나오는 게 의도임(사건번호
   등은 텍스트로 못 뽑으니 그 이미지 자체를 보여주는 방식으로 커버).
6. **사건번호 등 법원 정보** — 이미지로만 존재해서 자동화 안 함(의도적). 필요하면 관리자가 매물별로
   수동 입력하는 필드를 만들지, 이대로 "블로그 원문 확인" 링크로 둘지 팀장님 확인.
7. ~~로고/도메인~~ → **로고 완료.** `assets/logo-dark.png`(어두운 배경용) 전 페이지 헤더/푸터에 적용함.
   도메인 이름은 여전히 미정 — 팀장님 확인 필요.
8. ~~회사소개·강의안내 카테고리~~ → **완료.** `about.html`(대표 소개, 핵심 지표, 회사 정보),
   `courses.html`(온라인 강의 22강 커리큘럼 + 오프라인 수업, liveklass 신청 링크 연결) 신규 추가하고
   전 페이지 네비게이션에 연결함. 내용은 `gkinvestmnet.imweb.me`(대표소개 정보), `gkinvestment.liveklass.com`
   (강의 커리큘럼·가격)에서 실제 확인한 내용 기반.
9. **후기(리뷰) 섹션 — 사용자 액션 필요.** liveklass 강의 페이지에 후기 탭이 따로 없었고, 웹 검색으로
   "옆커폰부동산에듀 후기"를 찾아봐도 무관한 결과(휴대폰 프랜차이즈 관련)만 나옴. naver 블로그도
   이 환경에서 접속이 막혀있어 확인 불가. 이전 교육원 사이트 때도 같은 이유로 막혔던 항목 — 카카오톡
   오픈채팅이나 카페에 있는 실제 후기를 캡처/텍스트로 직접 받아야 반영 가능.

## 6. 다음 세션 시작하면 이렇게

1. 이 파일 먼저 읽기.
2. 4번 "도구 제약"이 이번에도 있는지 먼저 확인 (naver 도메인 접속 가능 여부, node/wrangler 설치 여부).
3. 5번 남은 일 중 팀장님이 지시하는 것부터, 특히 **1번(블로그 샘플)**이 없으면 매물 파싱 정확도를
   더 못 올리니 최우선으로 요청.

---
*이 파일은 다른 세션/컴퓨터에서 작업을 이어받기 위한 핸드오프 문서다. 진행 상황이 바뀌면 3·5번을 갱신할 것.*
