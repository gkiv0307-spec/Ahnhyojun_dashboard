# 사장님 할 일 체크리스트

> 2026-09-01 갱신 — 사이트 통합 이후 기준입니다.

---

## 상황 정리 (꼭 먼저 읽어주세요)

`부동산경매.store` 에는 원래 **직원분이 만든 랜딩 페이지**가 GitHub Pages로 올라가 있었습니다.
그 사이트가 이 저장소 것보다 완성도가 높아서, **랜딩 페이지를 이 저장소로 가져와 홈으로 삼고**
여기서 만든 기능(유입 대시보드 · 지역별 물건 페이지 · 물건 상세 자동 생성)을 얹었습니다.

**중요:** 지금 도메인이 붙어 있는 GitHub Pages는 **서버가 없어서** 아래 기능이 절대 돌아가지 않습니다.

- 유입 분석 대시보드 (방문 기록 저장)
- 물건 상세 페이지 `/property/{글번호}`
- 지역별 페이지 `/area/daegu` 등
- 자동 사이트맵

→ 그래서 **Cloudflare Pages로 옮겨야** 합니다. 아래가 그 절차입니다.

---

## 🔴 1단계 — Cloudflare Pages에 사이트 올리기 (15분)

### 1-1. 프로젝트 만들기

1. [dash.cloudflare.com](https://dash.cloudflare.com) 로그인
2. 왼쪽 **Workers & Pages** → **Create** → **Pages** 탭 → **Connect to Git**
3. GitHub 연결 후 저장소 **`gkiv0307-spec/Ahnhyojun_dashboard`** 선택
4. 빌드 설정은 **전부 비워둡니다** (정적 사이트 + Functions라 빌드가 필요 없습니다)
   - Framework preset: `None`
   - Build command: (비움)
   - Build output directory: `/`
5. **Save and Deploy** → 1~2분 뒤 `xxxxx.pages.dev` 주소가 나옵니다

### 1-2. 임시 주소에서 먼저 확인

받은 `xxxxx.pages.dev` 주소로 아래를 열어보세요. **아직 도메인은 건드리지 않습니다.**

| 주소 | 정상이면 |
|---|---|
| `xxxxx.pages.dev` | 지금 부동산경매.store와 똑같은 홈 화면 |
| `xxxxx.pages.dev/area` | 지역별 물건 페이지 |
| `xxxxx.pages.dev/sitemap.xml` | 페이지 목록 XML |
| `xxxxx.pages.dev/api/properties` | 물건 데이터 ⚠️ 아래 확인 |

### 1-3. ⚠️ 가장 중요한 확인

`xxxxx.pages.dev/api/properties` 화면에서 `Ctrl+F` 로 **`regionCode`** 검색

- `"regionCode":"daegu"` 처럼 값이 있으면 → ✅ 정상
- `"regionCode":""` 로 비어 있으면 → ⚠️ **화면 캡처해서 알려주세요.** 지역 페이지가 안 열립니다

---

## 🔴 2단계 — 유입 대시보드 켜기 (10분)

1. Cloudflare → **Storage & Databases → D1** → **Create database** → 이름 `ykp-analytics`
2. 방금 만든 Pages 프로젝트 → **Settings → Bindings** → **Add binding**
   - Variable name: **`DB`** ← 대문자 두 글자, 오타 주의
   - D1 database: `ykp-analytics`
3. **Deployments** → 맨 위 배포 **⋯** → **Retry deployment**
4. `xxxxx.pages.dev` 를 한 번 방문한 뒤 `xxxxx.pages.dev/dashboard.html` 접속
   → 노란 "예시 데이터" 경고가 사라지면 완료
5. **Settings → Environment variables** → `DASHBOARD_KEY` = 아무 비밀번호 → 재배포
   → 대시보드 우측 상단 **[접근 키]** 에 같은 값 입력 (안 걸면 주소만 알면 누구나 봅니다)

---

## 🟡 3단계 — 도메인 옮기기 (마지막에!)

**1·2단계가 다 확인된 뒤에** 하세요. 순서를 바꾸면 사이트가 잠깐 비어 보일 수 있습니다.

### 먼저 확인할 것

도메인 `부동산경매.store` 를 **어디서 구입·관리**하고 계신가요? (가비아, 후이즈, Cloudflare 등)
그리고 그 계정이 **사장님 것**인가요, 직원분 것인가요?

> ⚠️ 지금 사이트가 직원분 GitHub에 있었으니, **도메인 계정도 직원분 것일 수 있습니다.**
> 그렇다면 도메인 관리 권한부터 넘겨받아야 합니다. 이게 안 되면 3단계는 진행할 수 없습니다.

### 옮기는 방법

1. Cloudflare Pages 프로젝트 → **Custom domains** → **Set up a domain**
2. `부동산경매.store` 입력
3. Cloudflare가 알려주는 대로:
   - 도메인 DNS가 이미 Cloudflare에 있으면 → 버튼 한 번으로 끝
   - 다른 곳(가비아 등)에 있으면 → 알려주는 CNAME/A 레코드를 그 사이트에 등록
4. 연결되면 `부동산경매.store` 로 새 사이트가 뜹니다

### 옮긴 뒤 확인

- `부동산경매.store/area` 가 열리는지
- `부동산경매.store/dashboard.html` 이 실제 데이터를 보여주는지
- 홈 화면이 예전과 똑같은지

---

## 🟢 4단계 — 검색엔진 (도메인 옮긴 뒤)

**네이버 소유확인 태그는 이미 사이트에 들어 있습니다**(직원분이 등록해 둔 것을 그대로 옮겼습니다).
서치어드바이저에 이미 등록돼 있다면 재등록할 필요 없이 **사이트맵만 다시 제출**하면 됩니다.

1. [searchadvisor.naver.com](https://searchadvisor.naver.com) → 웹마스터 도구 → 사이트 선택
   → **요청 → 사이트맵 제출** → `sitemap.xml`
   → **요청 → 웹페이지 수집** 에 `/`, `/area`, `/area/daegu` 각각 요청
2. [구글 서치콘솔](https://search.google.com/search-console) 등록 (아직 안 하셨다면)
   → 소유확인 **HTML 태그** 코드를 **저에게 보내주세요** → 넣어드립니다 → 사이트맵 제출

**구글 애널리틱스(G-TF6QHZ61PZ)** 는 이미 전 페이지에 붙어 있습니다. 새로 만든 페이지들도 자동 집계됩니다.

---

## 🟢 5단계 — 계속 반복 (블로그 운영)

**글 제목**은 지역+동네+종류를 앞쪽에: `대구 북구 읍내동 아파트 경매 - 칠곡이편한세상 권리분석`

**본문 하단에 복붙:**
```
▶ 이 물건 상세보기: https://부동산경매.store/property/{이 글 번호}
▶ 대구 경매 물건 전체: https://부동산경매.store/area/daegu
▶ 지역별 경매 물건: https://부동산경매.store/area
```

**지역 확장은 자동입니다.** 그 지역 물건 글이 3건 쌓이면 지역 페이지가 저절로 열립니다.

| 목표 검색어 | 필요한 것 | 열리는 주소 |
|---|---|---|
| 부산아파트경매 | 부산 물건 글 3건 | `/area/busan` |
| 경남아파트경매 | 경남 물건 글 3건 | `/area/gyeongnam` |
| 경북아파트경매 | 경북 물건 글 3건 | `/area/gyeongbuk` |

---

## ⚪ 나중에

| 항목 | 내용 |
|---|---|
| **직원분 저장소 정리** | 도메인을 옮긴 뒤에는 예전 GitHub Pages 사이트가 쓰이지 않습니다. 앞으로 수정은 전부 이 저장소에서 합니다 |
| **결제 실계약** | 지금은 토스 테스트 키라 실제 결제가 안 됩니다. 가맹점 가입 후 키를 주시면 교체 |
| **영문 도메인 추가** | 한글 도메인은 카톡 공유 시 `xn--289av8kwmfs4dv2e.store` 로 깨져 보일 수 있습니다 |

---

## 지금 당장 할 3가지

1. **Cloudflare Pages 프로젝트 만들기** (이 저장소 연결, 빌드 설정 비움)
2. **`pages.dev/api/properties` 에서 `regionCode` 확인**
3. **D1 만들고 `DB` 로 바인딩 후 재배포**

그다음 **도메인 관리 계정이 사장님 것인지** 확인해 주시면 도메인 이전을 진행합니다.
