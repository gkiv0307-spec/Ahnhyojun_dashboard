# 사장님 할 일 체크리스트

지금까지 만든 것(유입 분석 대시보드 + 지역별 SEO 구조)을 **실제로 작동시키기 위해** 계정 권한이 필요한 작업들입니다.
코드로 되는 건 전부 끝났고, 아래는 사람이 직접 눌러야 하는 것들입니다.

---

## 🔴 1단계 — 오늘 (30분, 순서대로)

### 1-1. 코드를 사이트에 반영 (5분) ⭐ 이걸 먼저 안 하면 나머지가 다 무의미

지금 만든 것은 `claude/realestate-auction-traffic-dashboard-gyq3w4` **브랜치**에만 있습니다.

1. GitHub 저장소 접속 → 위쪽 노란 **"Compare & pull request"** 버튼
2. **Create pull request** → **Merge pull request** → **Confirm merge**
3. 1~2분 뒤 Cloudflare가 자동 배포

**확인:** `부동산경매.store/area` 가 열리면 성공.

### 1-2. 배포 상태 확인 (5분)

브라우저에서 아래 5개를 열어보세요.

| 주소 | 정상이면 |
|---|---|
| `부동산경매.store` | 매물 목록, 상단 메뉴에 "지역별 경매" 보임 |
| `부동산경매.store/area` | 지역 허브 |
| `부동산경매.store/sitemap.xml` | 글자 잔뜩 있는 XML |
| `부동산경매.store/robots.txt` | 짧은 텍스트 |
| `부동산경매.store/api/properties` | 매물 데이터 (⚠️ 아래 1-3 참고) |

### 1-3. ⚠️ 가장 중요한 확인 — 지역이 자동으로 붙는지

`부동산경매.store/api/properties` 를 열고 **`regionCode`** 라는 글자를 찾아보세요. (Ctrl+F)

- `"regionCode":"daegu"` 처럼 값이 있으면 → **정상.** 지역 페이지가 작동합니다
- `"regionCode":""` 로 비어 있으면 → **지역 페이지가 전부 404가 됩니다.** 화면을 캡처해서 저에게 보내주세요. 실제 블로그 글 문장에 맞게 판별 규칙을 고치겠습니다

> 이 환경에서는 네이버 접속이 막혀 실제 블로그로 검증을 못 했습니다. 배포 후 이 확인이 꼭 필요합니다.

### 1-4. 유입 대시보드 켜기 (10분)

`분석-대시보드-설정.md` 1번 그대로:

1. Cloudflare → **Storage & Databases → D1** → **Create database** → 이름 `ykp-analytics`
2. Cloudflare → **Workers & Pages** → 본인 사이트 → **Settings → Bindings** → **Add binding**
   - Variable name: **`DB`** (대문자 두 글자, 오타 주의)
   - D1 database: `ykp-analytics`
3. **Deployments** 탭 → 맨 위 배포 **⋯** → **Retry deployment**
4. 사이트 한 번 방문 후 `부동산경매.store/dashboard.html` 접속 → 노란 "예시 데이터" 경고가 사라지면 완료

### 1-5. 대시보드 잠그기 (5분)

같은 Settings → **Environment variables** → `DASHBOARD_KEY` = 아무 비밀번호 → 재배포
→ 대시보드 우측 상단 **[접근 키]** 버튼에 같은 값 입력

---

## 🟡 2단계 — 이번 주 (검색 등록, 각 10분)

### 2-1. 구글 서치콘솔

1. [search.google.com/search-console](https://search.google.com/search-console) → **속성 추가** → **URL 접두어**
2. `https://부동산경매.store` 입력
3. 소유권 확인 → **HTML 태그** 선택 → `<meta name="google-site-verification" content="...">` 복사
4. **그 줄을 저에게 보내주세요** → 제가 넣고 재배포 → 사장님이 "확인" 클릭
5. 확인되면 왼쪽 **Sitemaps** → `sitemap.xml` 입력 → 제출

### 2-2. 네이버 서치어드바이저 ⭐ 국내 검색은 이쪽이 핵심

1. [searchadvisor.naver.com](https://searchadvisor.naver.com) → **웹마스터 도구** → 사이트 등록
2. `https://부동산경매.store` 입력
3. 소유확인 → **HTML 태그** 방식 → 코드 복사 → **저에게 전달**
4. 확인 후 **요청 → 사이트맵 제출** → `sitemap.xml`
5. **요청 → 웹페이지 수집** 에 아래 3개 각각 넣고 수집 요청
   - `https://부동산경매.store/`
   - `https://부동산경매.store/area/daegu`
   - `https://부동산경매.store/courses.html`

> 네이버는 등록 후 노출까지 보통 **2~6주**. 블로그에서 링크가 계속 걸리면 빨라집니다.

### 2-3. 저에게 알려주실 정보 3가지

| 항목 | 이유 |
|---|---|
| **네이버 플레이스 주소(URL)** | 사이트 ↔ 플레이스를 연결해두면 "같은 업체"로 인식됩니다. 지역 검색에 도움 |
| **대표 이메일 확정** | 사이트엔 `rhghwjd12@naver.com` 이 들어가 있는데, 이전 문서엔 `hjko0219@kakao.com` 으로 되어 있습니다. 어느 쪽이 맞나요? |
| **위 검색엔진 소유확인 코드 2개** | 2-1, 2-2 |

---

## 🟢 3단계 — 계속 반복 (블로그 운영)

### 3-1. 블로그 글 제목 규칙

**지역 + 동네 + 종류 + 경매**를 제목 앞쪽에 넣어주세요. 이 제목이 그대로 매물 페이지 제목이 됩니다.

- 좋음: `대구 북구 읍내동 아파트 경매 - 칠곡이편한세상 권리분석`
- 아쉬움: `이번 물건 진짜 물건입니다`

### 3-2. 글 본문 아래 링크 (복붙용)

```
▶ 이 물건 상세보기: https://부동산경매.store/property/{이 글의 번호}
▶ 대구 경매 매물 전체: https://부동산경매.store/area/daegu
▶ 경매 강의 안내: https://부동산경매.store/courses.html
```

`{이 글의 번호}` = 블로그 글 주소 맨 뒤 숫자 (예: `blog.naver.com/ykphone_edu/224356311370` → `224356311370`)

어느 글이 사람을 데려왔는지 대시보드에서 보고 싶으면 뒤에 표시를 붙이세요.

```
https://부동산경매.store/area/daegu?utm_source=naver_blog&utm_medium=post&utm_campaign=대구경매
```

### 3-3. 지역 넓히기 — 따로 할 일 없음

블로그에 **그 지역 물건 3건**이 쌓이면 지역 페이지가 자동으로 열립니다.

| 목표 검색어 | 필요한 것 | 열리는 주소 |
|---|---|---|
| 부산아파트경매 | 부산 물건 글 3건 | `부동산경매.store/area/busan` |
| 경남아파트경매 | 경남 물건 글 3건 | `부동산경매.store/area/gyeongnam` |
| 경북아파트경매 | 경북 물건 글 3건 | `부동산경매.store/area/gyeongbuk` |

사이트맵에도 자동 등록되고, 검색엔진이 알아서 찾아갑니다.

---

## ⚪ 4단계 — 나중에 (급하지 않음)

| 항목 | 내용 |
|---|---|
| **결제 실계약** | 지금은 토스 "테스트 키"라 실제 결제가 안 됩니다. 가맹점 가입 후 키를 주시면 교체 |
| **결제 후 알림** | 신청이 들어와도 지금은 화면에만 표시됩니다. 문자·카톡·이메일 알림이 필요하면 말씀 주세요 |
| **수강생 후기 자료** | 실제 후기 캡처를 주시면 후기 페이지를 채웁니다 ("대구경매학원 후기" 검색 대응) |
| **정보성 글 페이지** | "경매 권리분석 방법" 같은 글을 사이트 페이지로 만들면 검색어를 더 잡습니다 |
| **영문 도메인 추가** | 한글 도메인은 카톡 공유 시 `xn--289av8kwmfs4dv2e.store` 로 깨져 보일 수 있습니다 |

---

## 요약 — 지금 당장 할 3가지

1. **GitHub에서 브랜치 머지** (안 하면 아무것도 반영 안 됨)
2. **`/api/properties` 열어서 `regionCode` 값이 있는지 확인** → 비어 있으면 알려주기
3. **Cloudflare에서 D1 만들고 변수명 `DB`로 연결 후 재배포**

그다음 검색엔진 2곳 등록하고 소유확인 코드만 전달해 주시면 됩니다.
