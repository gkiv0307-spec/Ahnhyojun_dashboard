# 검색 노출(SEO) 안내

목표: **"대구경매학원", "대구아파트경매", "부산아파트경매"** 처럼 지역+키워드 검색에서 이 사이트가 나오게 하는 것.
사이트를 여러 개 복제하지 않고 **한 도메인(부동산경매.store) 안에 지역 페이지를 늘려가는 방식**으로 만들었습니다.

---

## 1. 페이지 구조

| 주소 | 노리는 검색어 | 만들어지는 방식 |
|---|---|---|
| `/` | 대구 아파트 경매 매물 | 고정 페이지 |
| `/area` | 지역별 경매 매물 | 자동 (지역 허브) |
| `/area/daegu` | **대구아파트경매**, 대구경매물건 | 자동 (매물 3건 이상 지역만) |
| `/area/busan`, `/area/gyeongnam` … | 부산아파트경매, 경남아파트경매 | 자동 (해당 지역 매물이 3건 넘으면 열림) |
| `/property/{글번호}` | 단지명·동네 이름 검색 | 자동 (블로그 글 1건 = 페이지 1개) |
| `/courses.html` | **대구경매학원**, 대구 경매 강의 | 고정 페이지 |
| `/about.html` | 옆커폰부동산에듀 | 고정 페이지 |

지역 코드: `seoul busan daegu incheon gwangju daejeon ulsan sejong gyeonggi gangwon chungbuk chungnam jeonbuk jeonnam gyeongbuk gyeongnam jeju`

### 지역 페이지가 열리는 규칙
- 블로그 글의 **주소 문장과 해시태그**로 시·도를 자동 판별합니다.
- 한 지역에 매물이 **3건 이상** 모이면 그 지역 페이지가 자동으로 열리고 사이트맵에도 올라갑니다.
- 3건 미만이면 일부러 열지 않습니다(내용 빈약한 페이지는 사이트 전체 평가를 깎습니다). `/area` 에 "준비 중"으로 표시됩니다.
- 즉 **부산 물건 3건만 올리시면 `부동산경매.store/area/busan` 이 저절로 생깁니다.** 따로 작업할 게 없습니다.

---

## 2. 이번에 바뀐 것

- **매물 상세를 서버에서 완성해 내려줍니다.** 예전 `property.html?id=123` 은 자바스크립트로 그려서 네이버 크롤러가 내용을 못 읽었습니다. 이제 `/property/123` 이 글자 그대로 담긴 HTML을 내려줍니다. (옛 주소로 들어와도 새 주소로 자동 이동합니다.)
- **페이지마다 제목·설명이 달라졌습니다.** 검색결과에 그대로 노출되는 문구입니다.
- **`sitemap.xml` 자동 생성** — 매물이 늘면 자동으로 목록에 추가됩니다.
- **`robots.txt`** — 대시보드·결제 페이지는 검색에서 제외했습니다.
- **구조화 데이터(JSON-LD)** — 구글이 "대구에 있는 부동산 경매 교육 사업체", "이건 매물 목록", "이건 강의"로 이해하도록 표시했습니다.
- **카톡·문자 공유 이미지(OG)** 추가 — 링크를 보내면 썸네일이 뜹니다.
- **내부 링크 정리** — 홈 → 지역 허브 → 지역 페이지 → 매물 상세로 크롤러가 걸어 들어갈 길을 만들었습니다.

---

## 3. 지금 하셔야 할 일

### ① 구글 서치콘솔 등록
1. [search.google.com/search-console](https://search.google.com/search-console) 접속 → **속성 추가** → **URL 접두어**
2. `https://부동산경매.store` 입력
3. 소유권 확인에서 **HTML 태그** 방식을 고르면 `<meta name="google-site-verification" content="......">` 를 줍니다.
   → **그 줄을 저에게 보내주시면 사이트에 넣어 드립니다.** (또는 파일 방식으로 받은 `google......html` 파일을 저장소 맨 위에 올려도 됩니다)
4. 확인 완료 후 왼쪽 **Sitemaps** → `sitemap.xml` 입력 → 제출

### ② 네이버 서치어드바이저 등록
1. [searchadvisor.naver.com](https://searchadvisor.naver.com) 접속 → **웹마스터 도구** → 사이트 등록
2. `https://부동산경매.store` 입력
3. 소유확인은 **HTML 태그** 또는 **HTML 파일** — 받은 내용을 저에게 주시면 넣어 드립니다
4. 등록 후 **요청 → 사이트맵 제출** 에 `sitemap.xml` 제출
5. **요청 → 웹페이지 수집** 에 주요 주소를 하나씩 넣어 수집 요청 (`/`, `/area/daegu`, `/courses.html`)

> 네이버는 등록만으로 바로 노출되지 않고 보통 **2~6주** 걸립니다. 블로그에서 링크가 계속 걸리면 빨라집니다.

### ③ 블로그 글에 링크 걸기 (가장 효과 큼)
매물 글을 올리실 때 본문 아래에 이런 식으로 넣어주세요.

```
▶ 이 물건 상세: https://부동산경매.store/property/{그 글의 번호}
▶ 대구 경매 매물 전체: https://부동산경매.store/area/daegu
▶ 경매 강의 안내: https://부동산경매.store/courses.html
```

유입 대시보드에서 어느 글이 실제로 사람을 데려왔는지 보고 싶으시면 뒤에 표시를 붙이세요.

```
https://부동산경매.store/area/daegu?utm_source=naver_blog&utm_medium=post&utm_campaign=대구경매
```

---

## 4. 키워드별로 어떤 페이지가 싸우는지

| 검색어 | 담당 페이지 | 같이 필요한 것 |
|---|---|---|
| 대구경매학원 / 대구 경매 강의 | `/courses.html` | **네이버 플레이스**(등록 완료) + 블로그 후기 글 |
| 대구아파트경매 | `/area/daegu` | 대구 매물 글 꾸준히 |
| 부산아파트경매 | `/area/busan` | 부산 물건 3건 이상 |
| 경남아파트경매 | `/area/gyeongnam` | 경남 물건 3건 이상 |
| 단지명 (예: 칠곡이편한세상 경매) | `/property/{글번호}` | 글 제목에 단지명 넣기 |
| 옆커폰부동산에듀 | `/`, `/about.html` | — |

**중요:** 네이버에서 "대구경매학원" 같은 지역+업체 검색은 상단이 **플레이스와 블로그**입니다. 사이트는 그 아래 웹사이트/웹문서 영역에서 잡히므로, 플레이스·블로그와 사이트가 **셋 다** 있어야 검색면을 넓게 덮습니다.

---

## 5. 앞으로 더 하면 좋은 것

1. **블로그 글 제목에 지역+키워드를 앞쪽에 배치** — "대구 북구 읍내동 아파트 경매" 처럼. 이 제목이 그대로 매물 페이지 제목이 됩니다.
2. **매물이 아닌 정보성 글**(권리분석 방법, 명도 절차 등)도 사이트에 페이지로 만들면 "경매 권리분석" 같은 검색어를 잡을 수 있습니다. 필요하시면 만들어 드립니다.
3. **후기 페이지 보강** — 실제 후기가 많을수록 "대구경매학원 후기" 검색을 잡습니다.
4. **영문 도메인 추가 연결** — 한글 도메인은 카톡·문자로 공유될 때 `xn--289av8kwmfs4dv2e.store` 로 깨져 보일 수 있습니다.

---

## 6. 확인 방법

배포 후 아래 주소가 정상인지 봐주세요.

| 주소 | 기대 결과 |
|---|---|
| `부동산경매.store/sitemap.xml` | 페이지 목록 XML |
| `부동산경매.store/robots.txt` | 크롤러 안내 문서 |
| `부동산경매.store/area` | 지역 허브 |
| `부동산경매.store/area/daegu` | 대구 매물 목록 (매물 3건 이상일 때) |
| `부동산경매.store/property/224356311370` | 매물 상세 (실제 글번호로) |

`/area/daegu` 가 "준비 중"으로 뜨면 아직 대구 매물이 3건 미만이거나 블로그 RSS 파싱이 안 된 것입니다.
`/api/properties` 를 열어 `items` 안에 매물이 몇 건 잡히는지 확인해 보세요.
