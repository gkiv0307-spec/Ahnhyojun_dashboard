# 유입 분석 대시보드 — 설정 안내

부동산경매.store 방문자가 **어디서 / 어떤 경로로** 들어오는지 보는 대시보드입니다.
외부 분석 서비스 없이, 사이트가 이미 올라가 있는 **Cloudflare Pages + D1**만으로 돌아갑니다.

- 대시보드 주소: `https://부동산경매.store/dashboard.html`
- 구성 파일
  | 파일 | 역할 |
  |---|---|
  | `assets/track.js` | 방문자 브라우저에서 유입 정보를 수집(모든 페이지에 삽입 완료) |
  | `functions/api/track.js` | 수집 데이터를 받아 D1에 저장 |
  | `functions/api/stats.js` | 대시보드용 집계 JSON 제공 |
  | `dashboard.html`, `assets/dashboard.css` | 대시보드 화면 |
  | `schema.sql` | D1 테이블 정의(수동 실행 안 해도 됨) |

> **중요:** 분석은 **이 코드가 배포된 다음 발생한 방문부터** 쌓입니다.
> 지금까지의 과거 방문 기록은 어디에도 남아 있지 않아 소급해서 볼 수 없습니다.

---

## 1. D1 데이터베이스 연결 (한 번만, 약 3분)

**A. 데이터베이스 만들기**
Cloudflare 대시보드 → **Workers & Pages → D1 → Create database**
이름 예: `ykp-analytics`
(터미널을 쓴다면 `npx wrangler d1 create ykp-analytics`)

**B. Pages 프로젝트에 연결**
Pages 프로젝트 → **Settings → Functions(또는 Bindings) → D1 database bindings → Add binding**

| 항목 | 값 |
|---|---|
| Variable name | `DB` ← **반드시 대문자 DB** |
| D1 database | 방금 만든 `ykp-analytics` |

Production과 Preview 양쪽에 모두 추가하세요.

**C. 재배포**
바인딩은 **새 배포부터** 적용됩니다. Deployments에서 Retry deployment를 누르거나 아무 커밋이나 push하세요.

**D. 확인**
1. 사이트(`https://부동산경매.store`)를 한 번 열어 방문 기록을 만든다.
2. `https://부동산경매.store/api/stats?days=7` 를 열어 JSON에 숫자가 보이는지 본다.
   - `"configured": false` → B단계 바인딩 이름이 `DB`가 맞는지 확인
   - `"empty": true` → 아직 수집된 방문이 없음(1번을 다시)
3. `https://부동산경매.store/dashboard.html` 접속.

테이블(`events`)은 첫 방문이 들어오는 순간 자동으로 만들어집니다. `schema.sql`을 직접 실행할 필요는 없습니다.

---

## 2. 대시보드 잠그기 (권장)

기본 상태에서는 주소만 알면 누구나 대시보드를 볼 수 있습니다.

Pages → **Settings → Environment variables** 에 추가:

| 변수 | 값 |
|---|---|
| `DASHBOARD_KEY` | 아무도 모를 문자열 (예: `ykp-2026-8gj3x`) |

재배포한 뒤 대시보드 오른쪽 위 **[접근 키]** 버튼을 눌러 같은 값을 입력하면, 그 브라우저에는 저장되어 다음부터 바로 열립니다.

선택 변수:

| 변수 | 기본값 | 설명 |
|---|---|---|
| `RETENTION_DAYS` | `180` | 이 일수보다 오래된 기록은 자동 삭제 |

---

## 3. 대시보드 보는 법

| 항목 | 뜻 |
|---|---|
| **방문자** | 브라우저 단위 사람 수(익명 ID 기준) |
| **방문(세션)** | 방문 횟수. 30분 이상 활동이 없으면 다음 접속은 새 방문 |
| **유입 채널** | 방문이 시작된 출처를 묶은 것 — 네이버 블로그 / 네이버 검색 / 네이버 카페 / 카카오톡 / 구글 검색 / 다음 / 인스타그램 / 유튜브 / 밴드 / 직접 유입 / 유료광고 등 |
| **리퍼러 도메인** | 직전에 머물던 실제 주소(`blog.naver.com` 등) |
| **첫 진입 페이지** | 사이트에서 가장 먼저 열린 페이지 |
| **방문 경로** | 유입 채널 → 실제로 이동한 페이지 순서(최대 4단계)를 같은 경로끼리 묶은 것 |
| **1페이지 이탈률** | 한 페이지만 보고 나간 방문의 비율 |
| **전환 버튼 클릭** | 전화상담 / 강의 신청 / 결제 페이지 / 블로그 이동 클릭 수 |

기간 버튼(오늘·7일·30일·90일), **표로 보기**(모든 차트를 숫자표로), **CSV 내보내기**(엑셀용)를 지원합니다.

### 카카오톡·네이버 앱에서 들어온 방문
카톡으로 링크를 받아 열면 브라우저가 referrer를 안 넘겨주는 경우가 많습니다.
이때는 앱 종류(User-Agent)로 판별해 `카카오톡`, `네이버 앱`으로 분류합니다.
그래도 구분이 안 되는 것은 `직접 유입`으로 잡힙니다.

### 링크에 UTM 붙이기 (권장)
어디서 온 유입인지 확실히 나누고 싶으면 뿌리는 링크 뒤에 표시를 붙이세요.

```
https://부동산경매.store/?utm_source=naver_blog&utm_medium=post&utm_campaign=2026-08-경매특강
https://부동산경매.store/courses.html?utm_source=kakao&utm_medium=sms&utm_campaign=수강생-안내
```

- `utm_source` 어디에 올린 링크인지 (naver_blog, kakao, instagram …)
- `utm_medium` 형태 (post, sms, cpc …)
- `utm_campaign` 무엇을 위한 것인지

이렇게 뿌린 링크는 대시보드 **캠페인(UTM)** 표에 따로 집계됩니다.

---

## 4. 수집 항목과 개인정보

**저장하는 것**: 익명 난수 ID(방문자·세션), 방문 시각, 페이지 주소, 유입 referrer/UTM,
기기·브라우저 종류, Cloudflare가 알려주는 국가·시도.

**저장하지 않는 것**: **IP 주소**, 이름, 연락처, 입력값, 결제 정보.

- 사용자의 브라우저 저장소(localStorage)에 익명 ID를 남깁니다.
- 봇·크롤러로 보이는 접속은 저장하지 않습니다.
- 데이터는 Cloudflare D1의 `events` 테이블에만 남고 외부로 나가지 않습니다.
- 개인정보처리방침 페이지를 만들 때 "웹로그 분석 도구를 자체 운영하며 IP는 저장하지 않음" 정도로 적으면 됩니다.

---

## 5. 알아둘 한계

- 배포 이후 방문부터만 집계됩니다(소급 불가).
- 광고 차단 확장, 시크릿 모드, 저장소 차단 환경에서는 일부가 누락됩니다.
- 같은 사람이 폰·PC로 각각 들어오면 방문자 2명으로 잡힙니다.
- 카톡·문자로 받은 링크는 referrer가 비는 경우가 있어 일부가 `직접 유입`에 섞입니다.
- `/dashboard.html` 자체 방문은 집계에서 제외되며, 검색엔진에는 노출되지 않게(`noindex`) 해두었습니다.

---

## 6. 자주 쓰는 관리 명령

```bash
# 최근 저장된 20건 확인
npx wrangler d1 execute ykp-analytics --remote \
  --command "SELECT day, hour, channel, ref_domain, landing_path, path FROM events ORDER BY id DESC LIMIT 20"

# 전체 방문 수
npx wrangler d1 execute ykp-analytics --remote \
  --command "SELECT COUNT(*) pv, COUNT(DISTINCT session_id) sessions FROM events"

# 테스트로 쌓인 기록 전부 지우기
npx wrangler d1 execute ykp-analytics --remote --command "DELETE FROM events"
```

---

## 7. (참고) 구글 애널리틱스를 함께 쓰고 싶다면

이 대시보드와 별개로 GA4를 붙여도 충돌하지 않습니다.
GA4 측정 ID를 받은 뒤 각 페이지 `</head>` 앞에 gtag 스니펫을 넣으면 됩니다.
다만 GA4는 네이버 블로그/카페, 카카오톡 인앱 구분이 이 대시보드만큼 세밀하지 않고,
데이터가 구글 서버에 저장된다는 차이가 있습니다.
